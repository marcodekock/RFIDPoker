# Copilot Instructions

## Project Guidelines
- Always run `ng build` from the RFIDPoker.Client directory after completing any work that touches the Angular app, so the wwwroot bundle stays in sync with source changes.